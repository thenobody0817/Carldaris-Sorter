/* Generated by scripts/build-standalone.cjs. Do not edit directly. Run "npm run build". */
(function () {
'use strict';

// ---- src/catalog.js ----
const LANGS = {
  ru: { name:'Русский',  locale:'ru-RU', speech:'ru-RU' },
  en: { name:'English',  locale:'en-US', speech:'en-US' },
  lv: { name:'Latviešu', locale:'lv-LV', speech:'lv-LV' },
  zh: { name:'中文',      locale:'zh-CN', speech:'zh-CN' },
};

const I18N = {
  ru: {
    'app.title':'Подсчёт',
    'app.untitled':'Без названия',
    'voice.title':'Голосовой ввод',
    'settings.title':'Настройки',
    'categories.title':'Категории',
    'csv':'CSV',
    'reset':'Обнулить',
    'save':'💾 Сохранить',
    'more':'Ещё',
    'close':'Закрыть',
    'calc.mulRows':'Умножить ряды',
    'photo.section':'Фото позиции',
    'photo.camera':'Снять фото',
    'photo.gallery':'Из галереи',
    'photo.remove':'🗑 Убрать фото',
    'edit':'✏️ Изменить',
    'delete':'🗑 Удалить',
    'calc.title':'Умножить',
    'calc.sub':'Ряды × В ряду',
    'calc.rows':'Рядов',
    'calc.cols':'В ряду',
    'calc.each':'Позиций в ящике',
    'calc.total':'Итого',
    'cancel':'Отмена',
    'calc.subtract':'− Вычесть',
    'calc.add':'+ Добавить',
    'multiply':'Умножить',
    'counter.reset':'Сброс',
    'settings.tileView':'Вид плиток',
    'settings.cols':'Плиток в ряду (столбцов)',
    'settings.auto':'Авто',
    'settings.rowsVisible':'Рядов на экране',
    'settings.direction':'Направление',
    'settings.ltr':'Слева → вправо',
    'settings.rtl':'Справа → влево',
    'settings.catAlign':'Название категории',
    'settings.alignLeft':'Слева',
    'settings.alignCenter':'По центру',
    'settings.alignRight':'Справа',
    'settings.textSize':'Размер текста',
    'settings.qtySize':'Размер счётчика',
    'settings.small':'Мелкий',
    'settings.medium':'Обычный',
    'settings.large':'Крупный',
    'settings.language':'Язык',
    'settings.theme':'Тема',
    'settings.themeAuto':'Авто',
    'settings.themeLight':'Светлая',
    'settings.themeDark':'Тёмная',
    'settings.themeOled':'OLED',
    'settings.contrast':'Контраст',
    'settings.contrastNormal':'Обычный',
    'settings.contrastHigh':'Высокий',
    'settings.accent':'Акцентный цвет',
    'accent.green':'Зелёный',
    'accent.blue':'Синий',
    'accent.violet':'Фиолетовый',
    'accent.pink':'Розовый',
    'accent.amber':'Янтарный',
    'accent.cyan':'Голубой',
    'reorder.title':'Перестановка плиток',
    'reorder.on':'Перетащите плитки; бросайте в пустые ячейки, чтобы оставить пропуски',
    'reorder.done':'Порядок сохранён',
    'editor.newItem':'Новая позиция',
    'editor.subItem':'Введите название и категорию',
    'editor.name':'Название',
    'editor.namePh':'Например: Кега 20 л',
    'editor.category':'Категория',
    'editor.categoryPh':'Например: Кеги',
    'editor.nameCategory':'Название категории',
    'editor.nameCategoryPh':'Например: Кеги',
    'editor.newCategory':'Новая категория',
    'editor.subNewCategory':'Введите название раздела',
    'editor.editCategory':'Изменить категорию',
    'editor.subEditCategory':'Новое имя отобразится у всех её позиций',
    'editor.editItem':'Изменить позицию',
    'editor.subEditItem':'Переименуйте или смените категорию',
    'editor.save':'✓ Сохранить',
    'cats.sub':'Переименуйте, удалите или добавьте раздел',
    'cats.add':'+ Добавить категорию',
    'cats.empty':'Пока нет ни одной категории',
    'cats.rename':'Переименовать',
    'cats.delete':'Удалить',
    'cats.deleteEmptyTitle':'Удалить категорию?',
    'cats.deleteEmptyMsg':'Категория «{cat}» пуста.',
    'cats.hasTitle':'Категория «{cat}»',
    'cats.hasMsg':'Содержит {n} {word}. Что сделать?',
    'cats.delAll':'Удалить вместе с позициями',
    'cats.orphan':'Перенести в «Без категории»',
    'addTile.title':'Добавить позицию',
    'reset.title':'Начать новый подсчёт?',
    'reset.msg':'Название документа будет очищено, а все счётчики — обнулены.\nПозиции, категории, фото и настройки останутся.',
    'reset.confirm':'Начать заново',
    'delete.title':'Удалить позицию?',
    'delete.msg':'«{name}» будет удалена вместе с количеством и фото.',
    'delete.confirm':'Удалить',
    'save.savedTitle':'Сохранено',
    'save.savedMsg':'Файл перезаписан. Все изменения теперь внутри него.',
    'save.downloadTitle':'Файл сохранён',
    'save.downloadMsg':'Файл «{filename}» скачан в папку «Загрузки».\n\nЗамените им текущий файл приложения — тогда все изменения сохранятся внутри HTML и поедут вместе с ним на любое устройство.',
    'save.understood':'Понятно',
    'save.failMsg':'Не удалось собрать файл: {msg}',
    'validate.category':'Введите название категории',
    'validate.item':'Введите название позиции',
    'validate.dupCat':'Такая категория уже есть',
    'img.error':'Не удалось прочитать изображение',
    'csv.name':'Название',
    'csv.date':'Дата',
    'csv.category':'Категория',
    'csv.item':'Позиция',
    'csv.qty':'Количество',
    'slug.default':'подсчёт',
    'voice.off':'Голосовой ввод выключен',
    'voice.selectFirst':'Сначала выберите позицию',
    'voice.selected':'Выбрано: {name}',
    'voice.notUnderstood':'Не понял: {text}',
    'voice.unsupported':'Голосовой ввод не поддерживается. Откройте в Chrome, Edge или Safari.',
    'voice.listening':'Слушаю...',
    'voice.noMic':'Нет доступа к микрофону. Проверьте HTTPS и разрешения.',
    'items.one':'позицию',
    'items.few':'позиции',
    'items.many':'позиций',
  },
  en: {
    'app.title':'Count',
    'app.untitled':'Untitled',
    'voice.title':'Voice input',
    'settings.title':'Settings',
    'categories.title':'Categories',
    'csv':'CSV',
    'reset':'Reset',
    'save':'💾 Save',
    'more':'More',
    'close':'Close',
    'calc.mulRows':'Multiply rows',
    'photo.section':'Item photo',
    'photo.camera':'Take photo',
    'photo.gallery':'From gallery',
    'photo.remove':'🗑 Remove photo',
    'edit':'✏️ Edit',
    'delete':'🗑 Delete',
    'calc.title':'Multiply',
    'calc.sub':'Rows × Per row',
    'calc.rows':'Rows',
    'calc.cols':'Per row',
    'calc.each':'Items per box',
    'calc.total':'Total',
    'cancel':'Cancel',
    'calc.subtract':'− Subtract',
    'calc.add':'+ Add',
    'multiply':'Multiply',
    'counter.reset':'Reset',
    'settings.tileView':'Tile view',
    'settings.cols':'Tiles per row (columns)',
    'settings.auto':'Auto',
    'settings.rowsVisible':'Rows visible on screen',
    'settings.direction':'Direction',
    'settings.ltr':'Left → right',
    'settings.rtl':'Right → left',
    'settings.catAlign':'Category name',
    'settings.alignLeft':'Left',
    'settings.alignCenter':'Center',
    'settings.alignRight':'Right',
    'settings.textSize':'Text size',
    'settings.qtySize':'Counter size',
    'settings.small':'Small',
    'settings.medium':'Normal',
    'settings.large':'Large',
    'settings.language':'Language',
    'settings.theme':'Theme',
    'settings.themeAuto':'Auto',
    'settings.themeLight':'Light',
    'settings.themeDark':'Dark',
    'settings.themeOled':'OLED',
    'settings.contrast':'Contrast',
    'settings.contrastNormal':'Normal',
    'settings.contrastHigh':'High',
    'settings.accent':'Accent color',
    'accent.green':'Green',
    'accent.blue':'Blue',
    'accent.violet':'Violet',
    'accent.pink':'Pink',
    'accent.amber':'Amber',
    'accent.cyan':'Cyan',
    'reorder.title':'Rearrange tiles',
    'reorder.on':'Drag tiles to rearrange; drop into empty cells to leave gaps',
    'reorder.done':'Order saved',
    'editor.newItem':'New item',
    'editor.subItem':'Enter name and category',
    'editor.name':'Name',
    'editor.namePh':'E.g.: Keg 20 L',
    'editor.category':'Category',
    'editor.categoryPh':'E.g.: Kegs',
    'editor.nameCategory':'Category name',
    'editor.nameCategoryPh':'E.g.: Kegs',
    'editor.newCategory':'New category',
    'editor.subNewCategory':'Enter the section name',
    'editor.editCategory':'Edit category',
    'editor.subEditCategory':'The new name will appear on all its items',
    'editor.editItem':'Edit item',
    'editor.subEditItem':'Rename or change the category',
    'editor.save':'✓ Save',
    'cats.sub':'Rename, delete or add a section',
    'cats.add':'+ Add category',
    'cats.empty':'No categories yet',
    'cats.rename':'Rename',
    'cats.delete':'Delete',
    'cats.deleteEmptyTitle':'Delete category?',
    'cats.deleteEmptyMsg':'Category “{cat}” is empty.',
    'cats.hasTitle':'Category “{cat}”',
    'cats.hasMsg':'Contains {n} {word}. What to do?',
    'cats.delAll':'Delete with items',
    'cats.orphan':'Move to “Uncategorized”',
    'addTile.title':'Add item',
    'reset.title':'Start a new count?',
    'reset.msg':'The document name will be cleared and all counters reset.\nItems, categories, photos and settings will remain.',
    'reset.confirm':'Start over',
    'delete.title':'Delete item?',
    'delete.msg':'“{name}” will be deleted along with its quantity and photo.',
    'delete.confirm':'Delete',
    'save.savedTitle':'Saved',
    'save.savedMsg':'File overwritten. All changes are now inside it.',
    'save.downloadTitle':'File saved',
    'save.downloadMsg':'File “{filename}” was downloaded to “Downloads”.\n\nReplace the current app file with it — all changes will be stored inside the HTML and travel with it to any device.',
    'save.understood':'Got it',
    'save.failMsg':'Could not build the file: {msg}',
    'validate.category':'Enter a category name',
    'validate.item':'Enter an item name',
    'validate.dupCat':'This category already exists',
    'img.error':'Could not read the image',
    'csv.name':'Name',
    'csv.date':'Date',
    'csv.category':'Category',
    'csv.item':'Item',
    'csv.qty':'Quantity',
    'slug.default':'count',
    'voice.off':'Voice input off',
    'voice.selectFirst':'Select an item first',
    'voice.selected':'Selected: {name}',
    'voice.notUnderstood':'Did not understand: {text}',
    'voice.unsupported':'Voice input is not supported. Open in Chrome, Edge or Safari.',
    'voice.listening':'Listening...',
    'voice.noMic':'No microphone access. Check HTTPS and permissions.',
    'items.one':'item',
    'items.few':'items',
    'items.many':'items',
  },
  lv: {
    'app.title':'Skaitīšana',
    'app.untitled':'Bez nosaukuma',
    'voice.title':'Balss ievade',
    'settings.title':'Iestatījumi',
    'categories.title':'Kategorijas',
    'csv':'CSV',
    'reset':'Nodzēst',
    'save':'💾 Saglabāt',
    'more':'Vairāk',
    'close':'Aizvērt',
    'calc.mulRows':'Reizināt rindas',
    'photo.section':'Pozīcijas foto',
    'photo.camera':'Uzņemt foto',
    'photo.gallery':'No galerijas',
    'photo.remove':'🗑 Noņemt foto',
    'edit':'✏️ Mainīt',
    'delete':'🗑 Dzēst',
    'calc.title':'Reizināt',
    'calc.sub':'Rindas × Rindā',
    'calc.rows':'Rindas',
    'calc.cols':'Rindā',
    'calc.each':'Vienības kastē',
    'calc.total':'Kopā',
    'cancel':'Atcelt',
    'calc.subtract':'− Atņemt',
    'calc.add':'+ Pievienot',
    'multiply':'Reizināt',
    'counter.reset':'Atiestatīt',
    'settings.tileView':'Flīžu skats',
    'settings.cols':'Flīzes rindā (kolonnas)',
    'settings.auto':'Auto',
    'settings.rowsVisible':'Redzamās rindas ekrānā',
    'settings.direction':'Virziens',
    'settings.ltr':'No kreisās → uz labo',
    'settings.rtl':'No labās → uz kreiso',
    'settings.catAlign':'Kategorijas nosaukums',
    'settings.alignLeft':'Pa kreisi',
    'settings.alignCenter':'Centrā',
    'settings.alignRight':'Pa labi',
    'settings.textSize':'Teksta izmērs',
    'settings.qtySize':'Skaitītāja izmērs',
    'settings.small':'Mazs',
    'settings.medium':'Parasts',
    'settings.large':'Liels',
    'settings.language':'Valoda',
    'settings.theme':'Tēma',
    'settings.themeAuto':'Auto',
    'settings.themeLight':'Gaišā',
    'settings.themeDark':'Tumšā',
    'settings.themeOled':'OLED',
    'settings.contrast':'Kontrasts',
    'settings.contrastNormal':'Parasts',
    'settings.contrastHigh':'Augsts',
    'settings.accent':'Akcenta krāsa',
    'accent.green':'Zaļš',
    'accent.blue':'Zils',
    'accent.violet':'Violets',
    'accent.pink':'Rozā',
    'accent.amber':'Dzintara',
    'accent.cyan':'Ciāna',
    'reorder.title':'Pārkārtot flīzes',
    'reorder.on':'Velciet flīzes; nometiet tukšās šūnās, lai atstātu atstarpes',
    'reorder.done':'Secība saglabāta',
    'editor.newItem':'Jauna pozīcija',
    'editor.subItem':'Ievadiet nosaukumu un kategoriju',
    'editor.name':'Nosaukums',
    'editor.namePh':'Piemēram: Kega 20 l',
    'editor.category':'Kategorija',
    'editor.categoryPh':'Piemēram: Kegas',
    'editor.nameCategory':'Kategorijas nosaukums',
    'editor.nameCategoryPh':'Piemēram: Kegas',
    'editor.newCategory':'Jauna kategorija',
    'editor.subNewCategory':'Ievadiet sadaļas nosaukumu',
    'editor.editCategory':'Mainīt kategoriju',
    'editor.subEditCategory':'Jaunais nosaukums parādīsies visām tās pozīcijām',
    'editor.editItem':'Mainīt pozīciju',
    'editor.subEditItem':'Pārdēvējiet vai mainiet kategoriju',
    'editor.save':'✓ Saglabāt',
    'cats.sub':'Pārdēvējiet, dzēsiet vai pievienojiet sadaļu',
    'cats.add':'+ Pievienot kategoriju',
    'cats.empty':'Vēl nav nevienas kategorijas',
    'cats.rename':'Pārdēvēt',
    'cats.delete':'Dzēst',
    'cats.deleteEmptyTitle':'Dzēst kategoriju?',
    'cats.deleteEmptyMsg':'Kategorija “{cat}” ir tukša.',
    'cats.hasTitle':'Kategorija “{cat}”',
    'cats.hasMsg':'Satur {n} {word}. Ko darīt?',
    'cats.delAll':'Dzēst kopā ar pozīcijām',
    'cats.orphan':'Pārvietot uz “Bez kategorijas”',
    'addTile.title':'Pievienot pozīciju',
    'reset.title':'Sākt jaunu skaitīšanu?',
    'reset.msg':'Dokumenta nosaukums tiks notīrīts, un visi skaitītāji tiks nodzēsti.\nPozīcijas, kategorijas, foto un iestatījumi paliks.',
    'reset.confirm':'Sākt no jauna',
    'delete.title':'Dzēst pozīciju?',
    'delete.msg':'“{name}” tiks dzēsta kopā ar daudzumu un foto.',
    'delete.confirm':'Dzēst',
    'save.savedTitle':'Saglabāts',
    'save.savedMsg':'Fails pārrakstīts. Visas izmaiņas tagad ir tajā.',
    'save.downloadTitle':'Fails saglabāts',
    'save.downloadMsg':'Fails “{filename}” lejupielādēts mapē “Lejupielādes”.\n\nAizstājiet ar to pašreizējo lietotnes failu — tad visas izmaiņas saglabāsies HTML iekšienē un ceļos kopā ar to uz jebkuru ierīci.',
    'save.understood':'Saprotu',
    'save.failMsg':'Neizdevās izveidot failu: {msg}',
    'validate.category':'Ievadiet kategorijas nosaukumu',
    'validate.item':'Ievadiet pozīcijas nosaukumu',
    'validate.dupCat':'Šāda kategorija jau pastāv',
    'img.error':'Neizdevās nolasīt attēlu',
    'csv.name':'Nosaukums',
    'csv.date':'Datums',
    'csv.category':'Kategorija',
    'csv.item':'Pozīcija',
    'csv.qty':'Daudzums',
    'slug.default':'skaitisana',
    'voice.off':'Balss ievade izslēgta',
    'voice.selectFirst':'Vispirms izvēlieties pozīciju',
    'voice.selected':'Izvēlēts: {name}',
    'voice.notUnderstood':'Nesapratu: {text}',
    'voice.unsupported':'Balss ievade netiek atbalstīta. Atveriet Chrome, Edge vai Safari.',
    'voice.listening':'Klausos...',
    'voice.noMic':'Nav piekļuves mikrofonam. Pārbaudiet HTTPS un atļaujas.',
    'items.one':'pozīciju',
    'items.few':'pozīcijas',
    'items.many':'pozīcijas',
  },
  zh: {
    'app.title':'计数',
    'app.untitled':'无标题',
    'voice.title':'语音输入',
    'settings.title':'设置',
    'categories.title':'分类',
    'csv':'CSV',
    'reset':'清零',
    'save':'💾 保存',
    'more':'更多',
    'close':'关闭',
    'calc.mulRows':'相乘行数',
    'photo.section':'商品照片',
    'photo.camera':'拍照',
    'photo.gallery':'从相册选择',
    'photo.remove':'🗑 移除照片',
    'edit':'✏️ 编辑',
    'delete':'🗑 删除',
    'calc.title':'相乘',
    'calc.sub':'行数 × 每行',
    'calc.rows':'行数',
    'calc.cols':'每行',
    'calc.each':'每箱数量',
    'calc.total':'合计',
    'cancel':'取消',
    'calc.subtract':'− 减去',
    'calc.add':'+ 添加',
    'multiply':'相乘',
    'counter.reset':'重置',
    'settings.tileView':'方块视图',
    'settings.cols':'每行方块数（列）',
    'settings.auto':'自动',
    'settings.rowsVisible':'屏幕可见行数',
    'settings.direction':'方向',
    'settings.ltr':'从左到右',
    'settings.rtl':'从右到左',
    'settings.catAlign':'分类名称',
    'settings.alignLeft':'左',
    'settings.alignCenter':'居中',
    'settings.alignRight':'右',
    'settings.textSize':'文字大小',
    'settings.qtySize':'计数器大小',
    'settings.small':'小',
    'settings.medium':'正常',
    'settings.large':'大',
    'settings.language':'语言',
    'settings.theme':'主题',
    'settings.themeAuto':'自动',
    'settings.themeLight':'浅色',
    'settings.themeDark':'深色',
    'settings.themeOled':'OLED',
    'settings.contrast':'对比度',
    'settings.contrastNormal':'普通',
    'settings.contrastHigh':'高',
    'settings.accent':'强调色',
    'accent.green':'绿色',
    'accent.blue':'蓝色',
    'accent.violet':'紫色',
    'accent.pink':'粉色',
    'accent.amber':'琥珀色',
    'accent.cyan':'青色',
    'reorder.title':'排列方块',
    'reorder.on':'拖动方块重新排列；放入空单元格可留出间隔',
    'reorder.done':'顺序已保存',
    'editor.newItem':'新项目',
    'editor.subItem':'输入名称和分类',
    'editor.name':'名称',
    'editor.namePh':'例如：20升桶',
    'editor.category':'分类',
    'editor.categoryPh':'例如：桶',
    'editor.nameCategory':'分类名称',
    'editor.nameCategoryPh':'例如：桶',
    'editor.newCategory':'新分类',
    'editor.subNewCategory':'输入分区名称',
    'editor.editCategory':'编辑分类',
    'editor.subEditCategory':'新名称将显示在其所有项目上',
    'editor.editItem':'编辑项目',
    'editor.subEditItem':'重命名或更改分类',
    'editor.save':'✓ 保存',
    'cats.sub':'重命名、删除或添加分区',
    'cats.add':'+ 添加分类',
    'cats.empty':'还没有任何分类',
    'cats.rename':'重命名',
    'cats.delete':'删除',
    'cats.deleteEmptyTitle':'删除分类？',
    'cats.deleteEmptyMsg':'分类“{cat}”是空的。',
    'cats.hasTitle':'分类“{cat}”',
    'cats.hasMsg':'包含 {n} {word}。要怎么做？',
    'cats.delAll':'连同项目一起删除',
    'cats.orphan':'移动到“未分类”',
    'addTile.title':'添加项目',
    'reset.title':'开始新的计数？',
    'reset.msg':'文档名称将被清除，所有计数器将归零。\n项目、分类、照片和设置将保留。',
    'reset.confirm':'重新开始',
    'delete.title':'删除项目？',
    'delete.msg':'“{name}”将连同数量和照片一起删除。',
    'delete.confirm':'删除',
    'save.savedTitle':'已保存',
    'save.savedMsg':'文件已覆盖。所有更改现在都在其中。',
    'save.downloadTitle':'文件已保存',
    'save.downloadMsg':'文件“{filename}”已下载到“下载”文件夹。\n\n用它替换当前应用文件——这样所有更改都会保存在 HTML 内，并随它一起到任何设备。',
    'save.understood':'知道了',
    'save.failMsg':'无法生成文件：{msg}',
    'validate.category':'请输入分类名称',
    'validate.item':'请输入项目名称',
    'validate.dupCat':'该分类已存在',
    'img.error':'无法读取图像',
    'csv.name':'名称',
    'csv.date':'日期',
    'csv.category':'分类',
    'csv.item':'项目',
    'csv.qty':'数量',
    'slug.default':'count',
    'voice.off':'语音输入已关闭',
    'voice.selectFirst':'请先选择一个项目',
    'voice.selected':'已选择：{name}',
    'voice.notUnderstood':'未听懂：{text}',
    'voice.unsupported':'不支持语音输入。请在 Chrome、Edge 或 Safari 中打开。',
    'voice.listening':'正在聆听...',
    'voice.noMic':'无法访问麦克风。请检查 HTTPS 和权限。',
    'items.one':'个项目',
    'items.few':'个项目',
    'items.many':'个项目',
  }
};

const DEFAULT_ITEMS = {
  ru: [
    {id:'crateGrnE', name:'Ящик зелёный',        category:'Ящики пустые'},
    {id:'crateBluE', name:'Ящик синий',          category:'Ящики пустые'},
    {id:'crateBluF', name:'Синий с прозрачными', category:'Ящики с бутылками'},
    {id:'crateTub',  name:'Синий с Tuborg',      category:'Ящики с бутылками'},
    {id:'crateGrnF', name:'Зелёный с бутылками', category:'Ящики с бутылками'},
    {id:'crateGrnGarage', name:'Зелёный с Garage', category:'Ящики с бутылками'},
    {id:'keg15',     name:'Кега 15 л',           category:'Кеги'},
    {id:'keg20',     name:'Кега 20 л',           category:'Кеги'},
    {id:'keg20slim', name:'Кега 20 л узкая',     category:'Кеги'},
    {id:'keg30',     name:'Кега 30 л',           category:'Кеги'},
    {id:'palEUR',    name:'Паллета EUR',         category:'Паллеты'},
  ],
  en: [
    {id:'crateGrnE', name:'Green crate',             category:'Empty crates'},
    {id:'crateBluE', name:'Blue crate',              category:'Empty crates'},
    {id:'crateBluF', name:'Blue with clear bottles', category:'Crates with bottles'},
    {id:'crateTub',  name:'Blue with Tuborg',        category:'Crates with bottles'},
    {id:'crateGrnF', name:'Green with bottles',      category:'Crates with bottles'},
    {id:'crateGrnGarage', name:'Green with Garage',  category:'Crates with bottles'},
    {id:'keg15',     name:'Keg 15 L',                category:'Kegs'},
    {id:'keg20',     name:'Keg 20 L',                category:'Kegs'},
    {id:'keg20slim', name:'Keg 20 L slim',           category:'Kegs'},
    {id:'keg30',     name:'Keg 30 L',                category:'Kegs'},
    {id:'palEUR',    name:'EUR pallet',              category:'Pallets'},
  ],
  lv: [
    {id:'crateGrnE', name:'Zaļā kaste',           category:'Tukšas kastes'},
    {id:'crateBluE', name:'Zilā kaste',           category:'Tukšas kastes'},
    {id:'crateBluF', name:'Zilā ar caurspīdīgām', category:'Kastes ar pudelēm'},
    {id:'crateTub',  name:'Zilā ar Tuborg',       category:'Kastes ar pudelēm'},
    {id:'crateGrnF', name:'Zaļā ar pudelēm',      category:'Kastes ar pudelēm'},
    {id:'crateGrnGarage', name:'Zaļā ar Garage',  category:'Kastes ar pudelēm'},
    {id:'keg15',     name:'Kega 15 l',            category:'Kegas'},
    {id:'keg20',     name:'Kega 20 l',            category:'Kegas'},
    {id:'keg20slim', name:'Kega 20 l šaurā',      category:'Kegas'},
    {id:'keg30',     name:'Kega 30 l',            category:'Kegas'},
    {id:'palEUR',    name:'Palete EUR',           category:'Paletes'},
  ],
  zh: [
    {id:'crateGrnE', name:'绿色箱',       category:'空箱'},
    {id:'crateBluE', name:'蓝色箱',       category:'空箱'},
    {id:'crateBluF', name:'蓝色带透明瓶', category:'带瓶箱'},
    {id:'crateTub',  name:'蓝色带Tuborg', category:'带瓶箱'},
    {id:'crateGrnF', name:'绿色带瓶',     category:'带瓶箱'},
    {id:'crateGrnGarage', name:'绿色带Garage', category:'带瓶箱'},
    {id:'keg15',     name:'15升桶',       category:'桶'},
    {id:'keg20',     name:'20升桶',       category:'桶'},
    {id:'keg20slim', name:'20升窄桶',     category:'桶'},
    {id:'keg30',     name:'30升桶',       category:'桶'},
    {id:'palEUR',    name:'EUR托盘',      category:'托盘'},
  ],
};

/* Hosted images live in the repo /images folder. Relative paths resolve both on
   GitHub Pages and when the file is opened locally next to the images folder. */
const IMAGE_BY_ID = {
  crateGrnE: 'images/crateGrnE.webp',
  crateBluE: 'images/crateBluE.webp',
  crateGrnF: 'images/crateGrnF.webp',
  crateBluF: 'images/crateBluF.webp',
  crateTub:  'images/crateTub.webp',
  crateGrnGarage: 'images/crateGrnGarage.webp',
  keg20:     'images/keg20.webp',
  keg20slim: 'images/keg20slim.webp',
  palEUR:    'images/palEUR.webp',
};

// ---- src/strings.js ----
const extra = {
  en: { sessions:'Sessions', newCount:'New count', untitled:'Untitled count', saved:'Saved on this device', offline:'Offline · saved on this device', yourItems:'Your items', hint:'Tap +1 · Swipe −1 · Hold for more', search:'Find an item…', total:'Total counted', checked:'checked', undo:'Undo', redo:'Redo', add:'Add item', selected:'Selected item', empty:'No items found.', backup:'Download backup', restore:'Restore backup', backupHint:'All sessions and settings, in a reusable JSON file.', restoreHint:'Replace current data with a backup. You can undo this.', export:'Export this count (CSV)', reset:'Clear this count', resetHint:'Sets every quantity to zero. You can undo this.', newHint:'Keeps this count in history and starts a fresh count with the same items.', name:'Name', edit:'Edit', remove:'Delete', ungrouped:'Other items', categories:'Categories', all:'All', details:'Item details', quantity:'Quantity', set:'Set quantity', bulk:'Add a stack', perRow:'Per row', rows:'Rows', apply:'Apply', move:'Reorder items', moveHint:'Use the arrows to move items within a category.', confirm:'Confirm', cancel:'Cancel', voice:'Voice counting', voiceHint:'Select an item, then say “plus five”. Speech availability depends on your browser and connection.', stopVoice:'Stop listening', reload:'Reload to update', ready:'Available offline', recovery:'Could not load saved data. Your saved data has not been overwritten. Download it for recovery or restore a valid backup.', rawBackup:'Download saved data for recovery' },
  lv: { sessions:'Sesijas', newCount:'Jauna uzskaite', untitled:'Uzskaite bez nosaukuma', saved:'Saglabāts šajā ierīcē', offline:'Bezsaistē · saglabāts ierīcē', yourItems:'Jūsu vienības', hint:'Pieskāriens +1 · Vilciens −1 · Turēt: vairāk', search:'Meklēt vienību…', total:'Kopā saskaitīts', checked:'pārbaudītas', undo:'Atsaukt', redo:'Atkārtot', add:'Pievienot', selected:'Izvēlētā vienība', empty:'Nav atrasta neviena vienība.', backup:'Lejupielādēt rezerves kopiju', restore:'Atjaunot rezerves kopiju', backupHint:'Visas sesijas un iestatījumi JSON failā.', restoreHint:'Aizstāt datus ar rezerves kopiju. To var atsaukt.', export:'Eksportēt uzskaiti (CSV)', reset:'Notīrīt šo uzskaiti', resetHint:'Iestata visus daudzumus uz nulli. To var atsaukt.', newHint:'Saglabā pašreizējo uzskaiti vēsturē un sāk jaunu ar tām pašām vienībām.', name:'Nosaukums', edit:'Rediģēt', remove:'Dzēst', ungrouped:'Citas vienības', categories:'Kategorijas', details:'Vienības dati', quantity:'Daudzums', set:'Iestatīt daudzumu', bulk:'Pievienot kaudzi', perRow:'Vienā rindā', rows:'Rindas', apply:'Lietot', move:'Mainīt secību', moveHint:'Izmantojiet bultiņas, lai mainītu secību kategorijā.', confirm:'Apstiprināt', cancel:'Atcelt', voice:'Balss uzskaite', voiceHint:'Izvēlieties vienību, tad sakiet “plus pieci”. Pieejamība atkarīga no pārlūka un savienojuma.', stopVoice:'Pārtraukt klausīšanos', reload:'Pārlādēt atjaunināšanai', ready:'Pieejams bezsaistē' },
  ru: { sessions:'Сессии', newCount:'Новый подсчёт', untitled:'Подсчёт без названия', saved:'Сохранено на устройстве', offline:'Без сети · сохранено на устройстве', yourItems:'Ваши позиции', hint:'Касание +1 · Смахивание −1 · Удержание: ещё', search:'Найти позицию…', total:'Всего посчитано', checked:'проверено', undo:'Отменить', redo:'Повторить', add:'Добавить', selected:'Выбранная позиция', empty:'Ничего не найдено.', backup:'Скачать резервную копию', restore:'Восстановить копию', backupHint:'Все сессии и настройки в файле JSON.', restoreHint:'Заменить данные резервной копией. Можно отменить.', export:'Экспорт подсчёта (CSV)', reset:'Очистить этот подсчёт', resetHint:'Обнулить все количества. Можно отменить.', newHint:'Сохранить текущий подсчёт в истории и начать новый с теми же позициями.', name:'Название', edit:'Изменить', remove:'Удалить', ungrouped:'Другие позиции', categories:'Категории', details:'Позиция', quantity:'Количество', set:'Задать количество', bulk:'Добавить стопку', perRow:'В ряду', rows:'Рядов', apply:'Применить', move:'Порядок позиций', moveHint:'Перемещайте позиции стрелками внутри категории.', confirm:'Подтвердить', cancel:'Отмена', voice:'Голосовой подсчёт', voiceHint:'Выберите позицию и скажите «плюс пять». Доступность зависит от браузера и соединения.', stopVoice:'Остановить запись', reload:'Обновить приложение', ready:'Доступно без сети' },
  zh: { sessions:'记录', newCount:'新建计数', untitled:'未命名计数', saved:'已保存在此设备', offline:'离线 · 已保存在此设备', yourItems:'物品', hint:'点击 +1 · 滑动 −1 · 长按查看更多', search:'查找物品…', total:'总计', checked:'已检查', undo:'撤销', redo:'重做', add:'添加物品', selected:'选中的物品', empty:'未找到物品。', backup:'下载备份', restore:'恢复备份', backupHint:'所有记录和设置保存为 JSON 文件。', restoreHint:'用备份替换当前数据。可以撤销。', export:'导出计数 (CSV)', reset:'清空此计数', resetHint:'将所有数量设为零。可以撤销。', newHint:'保留当前记录，并使用相同物品开始新计数。', name:'名称', edit:'编辑', remove:'删除', ungrouped:'其他物品', categories:'类别', details:'物品详情', quantity:'数量', set:'设置数量', bulk:'批量添加', perRow:'每行', rows:'行数', apply:'应用', move:'调整顺序', moveHint:'使用箭头在类别内移动物品。', confirm:'确认', cancel:'取消', voice:'语音计数', voiceHint:'选择物品，然后说“加五”。可用性取决于浏览器和网络。', stopVoice:'停止聆听', reload:'重新加载更新', ready:'可离线使用' },
};
const deletion = {
  en: { deleteHint:'Delete this item and its quantity? You can undo this until the app is reloaded.', deleteCategoryHint:'Keep the items without a category, or delete the category and its items? You can undo this until the app is reloaded.' },
  lv: { deleteHint:'Dzēst šo vienību un tās daudzumu? To var atsaukt līdz lietotnes pārlādei.', deleteCategoryHint:'Saglabāt vienības bez kategorijas vai dzēst kategoriju kopā ar vienībām? To var atsaukt līdz pārlādei.' },
  ru: { deleteHint:'Удалить позицию и её количество? Можно отменить до перезагрузки приложения.', deleteCategoryHint:'Сохранить позиции без категории или удалить вместе с категорией? Можно отменить до перезагрузки.' },
  zh: { deleteHint:'删除此物品及其数量？重新加载应用前可以撤销。', deleteCategoryHint:'保留物品并移出类别，还是删除类别及其物品？重新加载前可以撤销。' },
};
function translate(lang, key) { return deletion[lang]?.[key] ?? extra[lang]?.[key] ?? I18N[lang]?.[key] ?? extra.en[key] ?? I18N.en[key] ?? key; }

// ---- src/model.js ----
const VERSION = 4;
const MAX_QUANTITY = 999999999;
const languages = ['en', 'lv', 'ru', 'zh'];
const uid = () => (crypto.randomUUID ? crypto.randomUUID() : 'id-' + Date.now().toString(36) + '-' + Math.random().toString(36).slice(2, 10));
function localDate(now = new Date()) {
  return `${now.getFullYear()}-${String(now.getMonth() + 1).padStart(2, '0')}-${String(now.getDate()).padStart(2, '0')}`;
}
function validDate(value) {
  if (typeof value !== 'string' || !/^\d{4}-\d{2}-\d{2}$/.test(value)) return false;
  const date = new Date(value + 'T12:00:00Z');
  return !Number.isNaN(date.getTime()) && date.toISOString().slice(0,10) === value;
}
function quantity(value) {
  if (typeof value !== 'number' && (typeof value !== 'string' || !/^\d+$/.test(value))) throw new Error('Enter a whole number.');
  const number = Number(value);
  if (!Number.isSafeInteger(number) || number < 0 || number > MAX_QUANTITY) throw new Error('Enter a whole number between 0 and 999,999,999.');
  return number;
}
const defaults = {
  lang: 'en', theme: 'auto', accent: 'green', contrast: 'normal', gridCols: 'auto',
  gridDir: 'ltr', textSize: 'md', qtySize: 'md', rowFit: 'auto', catAlign: 'left',
};
const choices = {
  lang: languages, theme: ['auto', 'light', 'dark', 'oled'], accent: ['green', 'blue', 'violet', 'pink', 'amber', 'cyan'],
  contrast: ['normal', 'high'], gridCols: ['auto', '3', '4', '5', '6'], gridDir: ['ltr', 'rtl'],
  textSize: ['sm', 'md', 'lg'], qtySize: ['sm', 'md', 'lg'], rowFit: ['auto', '3', '4', '5', '6'], catAlign: ['left', 'center', 'right'],
};
function preferences(raw = {}) {
  return Object.fromEntries(Object.entries(defaults).map(([key, fallback]) => [key, choices[key].includes(raw[key]) ? raw[key] : fallback]));
}
function defaultCategory(name) {
  for (const list of Object.values(DEFAULT_ITEMS)) {
    const item = list.find(item => item.category === name);
    if (item) return item.id;
  }
  return null;
}
function translatedDefaultName(id, name) {
  return Object.values(DEFAULT_ITEMS).some(list => list.some(item => item.id === id && item.name === name));
}
function migrateLegacy(raw) {
  if (!raw || !Array.isArray(raw.items)) throw new Error('The old count data is not valid.');
  const names = new Set((Array.isArray(raw.categories) ? raw.categories : []).filter(name => typeof name === 'string' && name));
  for (const item of raw.items) if (item && typeof item.category === 'string' && item.category) names.add(item.category);
  const categories = [...names].map(name => ({ id: uid(), name, defaultKey: defaultCategory(name) }));
  const seen = new Set();
  const items = raw.items.filter(item => item && typeof item === 'object').map(item => {
    let id = typeof item.id === 'string' && item.id ? item.id : uid();
    if (seen.has(id)) id = uid();
    seen.add(id);
    const value = Number(item.qty);
    return {
      id, name: typeof item.name === 'string' ? item.name : 'Item',
      defaultKey: translatedDefaultName(item.id, item.name) ? item.id : null,
      categoryId: categories.find(category => category.name === item.category)?.id || null,
      image: safeImage(item.image) || IMAGE_BY_ID[item.id] || '',
      qty: Number.isSafeInteger(value) && value >= 0 && value <= MAX_QUANTITY ? value : 0,
      counted: Boolean(item.counted || value > 0), spacer: Boolean(item.spacer),
    };
  });
  const session = { id: uid(), name: String(raw.label || ''), date: validDate(raw.date) ? raw.date : localDate(), categories, items };
  return { version: VERSION, revision: 0, savedAt: null, preferences: preferences(raw), activeId: session.id, sessions: [session] };
}
function createState(lang = 'en') {
  lang = languages.includes(lang) ? lang : 'en';
  return migrateLegacy({ lang, date: localDate(), items: DEFAULT_ITEMS[lang].map(item => ({ ...item, qty: 0, image: IMAGE_BY_ID[item.id] || '' })) });
}
function safeImage(value) {
  return typeof value === 'string' && (/^images\/[a-zA-Z0-9_-]+\.(webp|png|jpg|jpeg)$/.test(value) || (value.length < 3000000 && /^data:image\/(png|jpeg|webp);base64,[a-zA-Z0-9+/=]+$/.test(value))) ? value : '';
}
function validateState(raw) {
  if (!raw || raw.version !== VERSION || !Array.isArray(raw.sessions) || !raw.sessions.length || raw.sessions.length > 500) throw new Error('Unsupported or invalid backup.');
  const state = structuredClone(raw);
  state.preferences = preferences(raw.preferences);
  const sessions = new Set();
  for (const session of state.sessions) {
    if (typeof session.id !== 'string' || !session.id || sessions.has(session.id)) throw new Error('Invalid session ID.');
    sessions.add(session.id);
    if (typeof session.name !== 'string' || !validDate(session.date) || !Array.isArray(session.items) || !Array.isArray(session.categories)) throw new Error('Invalid session data.');
    if (session.items.length > 10000 || session.categories.length > 1000) throw new Error('Backup is too large.');
    const categories = new Set();
    for (const category of session.categories) {
      if (!category || typeof category.id !== 'string' || !category.id || categories.has(category.id) || typeof category.name !== 'string') throw new Error('Invalid category.');
      categories.add(category.id);
    }
    const ids = new Set();
    for (const item of session.items) {
      if (!item || typeof item.id !== 'string' || !item.id || ids.has(item.id) || typeof item.name !== 'string') throw new Error('Invalid item.');
      ids.add(item.id);
      if (item.categoryId !== null && !categories.has(item.categoryId)) throw new Error('An item refers to a missing category.');
      item.qty = quantity(item.qty);
      item.image = safeImage(item.image);
      item.counted = Boolean(item.counted);
      item.spacer = Boolean(item.spacer);
    }
  }
  if (!sessions.has(state.activeId)) throw new Error('Missing active session.');
  state.revision = Number.isSafeInteger(state.revision) && state.revision >= 0 ? state.revision : 0;
  return state;
}
const activeSession = state => state.sessions.find(session => session.id === state.activeId);
const itemName = (item, lang) => DEFAULT_ITEMS[lang]?.find(value => value.id === item.defaultKey)?.name || item.name;
const categoryName = (category, lang) => DEFAULT_ITEMS[lang]?.find(value => value.id === category.defaultKey)?.category || category.name;
function totals(session) {
  return session.items.reduce((sum, item) => item.spacer ? sum : ({ quantity: sum.quantity + item.qty, counted: sum.counted + Number(item.counted), items: sum.items + 1 }), { quantity: 0, counted: 0, items: 0 });
}
function applyAction(previous, action) {
  const next = structuredClone(previous);
  const session = activeSession(next);
  const item = session.items.find(item => item.id === action.id);
  switch (action.type) {
    case 'count':
      if (!item || item.spacer) throw new Error('Item not found.');
      if (!Number.isSafeInteger(action.delta)) throw new Error('Invalid quantity change.');
      item.qty = quantity(Math.max(0, item.qty + action.delta)); item.counted = true; break;
    case 'set':
      if (!item || item.spacer) throw new Error('Item not found.');
      item.qty = quantity(action.value); item.counted = true; break;
    case 'preferences': next.preferences = preferences({ ...next.preferences, ...action.value }); break;
    case 'rename-session': session.name = String(action.name).trim().slice(0, 160); break;
    case 'new-session': {
      const fresh = structuredClone(session);
      fresh.id = uid(); fresh.name = String(action.name || '').trim().slice(0, 160); fresh.date = localDate();
      fresh.items.forEach(item => { item.qty = 0; item.counted = false; });
      next.sessions.unshift(fresh); next.activeId = fresh.id; break;
    }
    case 'switch-session':
      if (!next.sessions.some(session => session.id === action.id)) throw new Error('Session not found.');
      next.activeId = action.id; break;
    case 'reset': session.items.forEach(item => { item.qty = 0; item.counted = false; }); break;
    case 'save-item': {
      const name = String(action.name || '').trim().slice(0, 160);
      if (!name) throw new Error('Enter an item name.');
      if (action.categoryId && !session.categories.some(category => category.id === action.categoryId)) throw new Error('Category not found.');
      if (item) { item.name = name; item.defaultKey = null; item.categoryId = action.categoryId || null; }
      else session.items.push({ id: uid(), name, defaultKey: null, categoryId: action.categoryId || null, qty: 0, counted: false, spacer: false, image: '' });
      break;
    }
    case 'delete-item': session.items = session.items.filter(item => item.id !== action.id); break;
    case 'save-category': {
      const name = String(action.name || '').trim().slice(0, 160);
      if (!name) throw new Error('Enter a category name.');
      if (session.categories.some(category => category.id !== action.id && categoryName(category, next.preferences.lang).toLocaleLowerCase() === name.toLocaleLowerCase())) throw new Error('This category already exists.');
      const category = session.categories.find(category => category.id === action.id);
      if (category) { category.name = name; category.defaultKey = null; }
      else session.categories.push({ id: uid(), name, defaultKey: null });
      break;
    }
    case 'delete-category':
      session.categories = session.categories.filter(category => category.id !== action.id);
      if (action.deleteItems) session.items = session.items.filter(item => item.categoryId !== action.id);
      else session.items.forEach(item => { if (item.categoryId === action.id) item.categoryId = null; });
      break;
    case 'move': {
      if (!item) throw new Error('Item not found.');
      const group = session.items.filter(value => value.categoryId === item.categoryId);
      const target = group[group.indexOf(item) + action.direction];
      if (target) {
        const a = session.items.indexOf(item), b = session.items.indexOf(target);
        [session.items[a], session.items[b]] = [session.items[b], session.items[a]];
      }
      break;
    }
    default: throw new Error('Unknown action.');
  }
  return validateState(next);
}
function csv(state) {
  const session = activeSession(state), lang = state.preferences.lang;
  const cell = value => {
    let text = String(value ?? '');
    if (/^[\s]*[=+@-]/.test(text)) text = "'" + text;
    return `"${text.replaceAll('"', '""')}"`;
  };
  const rows = [['Session', session.name], ['Date', session.date], [], ['Category', 'Item', 'Quantity', 'Checked']];
  for (const item of session.items.filter(item => !item.spacer)) {
    const category = session.categories.find(category => category.id === item.categoryId);
    rows.push([category ? categoryName(category, lang) : '', itemName(item, lang), item.qty, item.counted ? 'yes' : 'no']);
  }
  return '\uFEFF' + rows.map(row => row.map(cell).join(';')).join('\r\n');
}

// ---- src/storage.js ----
const STORAGE_KEY = 'countlist.v4';
function loadState(storage, lang) {
  const current = storage.getItem(STORAGE_KEY);
  if (current !== null) return { state: validateState(JSON.parse(current)), raw: current };
  const legacy = storage.getItem('countlist.v3');
  return { state: legacy ? migrateLegacy(JSON.parse(legacy)) : createState(lang), raw: null };
}
function createStore(storage, initial) {
  let state = initial.state, expected = initial.raw;
  const undo = [], redo = [];
  function persist(next) {
    if (storage.getItem(STORAGE_KEY) !== expected) throw new Error('Another tab changed these counts. Reload before continuing.');
    next = validateState(next);
    next.revision = state.revision + 1; next.savedAt = new Date().toISOString();
    const raw = JSON.stringify(next);
    storage.setItem(STORAGE_KEY, raw);
    expected = raw; state = next;
  }
  return {
    get state() { return state; },
    get canUndo() { return undo.length > 0; },
    get canRedo() { return redo.length > 0; },
    initialize() { if (expected === null) persist(state); },
    commit(next) {
      const previous = state; persist(next); undo.push(previous);
      if (undo.length > 30) undo.shift(); redo.length = 0;
    },
    undo() { if (undo.length) { const old = state; persist(undo.at(-1)); undo.pop(); redo.push(old); } },
    redo() { if (redo.length) { const old = state; persist(redo.at(-1)); redo.pop(); undo.push(old); } },
  };
}

// ---- src/voice.js ----
const RU_NUMS = {
  ноль:0, нуль:0,
  один:1, одна:1, одну:1, одно:1,
  два:2, две:2, двух:2,
  три:3, трех:3, трёх:3,
  четыре:4, четырех:4, четырёх:4,
  пять:5, пяти:5,
  шесть:6, шести:6,
  семь:7, семи:7,
  восемь:8, восьми:8,
  девять:9, девяти:9,
  десять:10, десяти:10,
  одиннадцать:11, двенадцать:12, тринадцать:13, четырнадцать:14,
  пятнадцать:15, шестнадцать:16, семнадцать:17, восемнадцать:18,
  девятнадцать:19,
  двадцать:20, тридцать:30, сорок:40, пятьдесят:50,
  шестьдесят:60, семьдесят:70, восемьдесят:80, девяносто:90,
  сто:100, двести:200, триста:300, четыреста:400,
  пятьсот:500, шестьсот:600, семьсот:700, восемьсот:800, девятьсот:900,
};

const EN_NUMS = {
  zero:0, one:1, two:2, three:3, four:4, five:5, six:6, seven:7, eight:8,
  nine:9, ten:10, eleven:11, twelve:12, thirteen:13, fourteen:14,
  fifteen:15, sixteen:16, seventeen:17, eighteen:18, nineteen:19,
  twenty:20, thirty:30, forty:40, fifty:50, sixty:60, seventy:70,
  eighty:80, ninety:90, hundred:100, thousand:1000
};

const LV_NUMS = {
  nulle:0, viens:1, viena:1, divi:2, divas:2, tris:3, trīs:3, cetri:4,
  četri:4, cetras:4, četras:4, pieci:5, piecas:5, sesi:6, seši:6,
  sesas:6, sešas:6, septini:7, septiņi:7, septinas:7, septiņas:7,
  astoni:8, astoņi:8, astonas:8, astoņas:8, devini:9, deviņi:9,
  devinas:9, deviņas:9, desmit:10, vienpadsmit:11, divpadsmit:12,
  trispadsmit:13, trīspadsmit:13, cetrpadsmit:14, četrpadsmit:14,
  piecpadsmit:15, sespadsmit:16, sešpadsmit:16, septinpadsmit:17,
  septiņpadsmit:17, astonpadsmit:18, astoņpadsmit:18, devinpadsmit:19,
  deviņpadsmit:19, divdesmit:20, trisdesmit:30, trīsdesmit:30,
  cetrdesmit:40, četrdesmit:40, piecdesmit:50, sesdesmit:60,
  sešdesmit:60, septindesmit:70, septiņdesmit:70, astondesmit:80,
  astoņdesmit:80, devindesmit:90, deviņdesmit:90, simts:100
};

const NUM_WORDS = { ru: RU_NUMS, en: EN_NUMS, lv: LV_NUMS };

const VOICE_WORDS = {
  ru: {
    stop:  /(^| )(стоп|хватит|закончить|выключи|выключить)( |$)/,
    plus:  /(^| )(плюс|добав|прибав|плюсуй)( |$)/,
    minus: /(^| )(минус|убери|уменьши|отними)( |$)/
  },
  en: {
    stop:  /(^| )(stop|finish|turn off|off)( |$)/,
    plus:  /(^| )(plus|add|increase)( |$)/,
    minus: /(^| )(minus|remove|subtract|decrease)( |$)/
  },
  lv: {
    stop:  /(^| )(stop|beigt|izslegt|izslēgt|izslēdz)( |$)/,
    plus:  /(^| )(plus|pievieno|pieliec|palielini)( |$)/,
    minus: /(^| )(minus|mīnus|atnem|atņem|nonem|noņem|samazini)( |$)/
  },
  zh: {
    stop:  /(停止|关闭|結束|结束|停)/,
    plus:  /(加|添加|增加|加上)/,
    minus: /(减|減|减去|減去|去掉|减少|減少)/
  }
};

const VOICE_STOP = {
  ru: new Set(['плюс','минус','добавь','прибавь','убери','отними','и','в','на','для','с','к','по','мл','шт','штук','штуки','л','литров','литра']),
  en: new Set(['plus','minus','add','remove','subtract','and','in','on','for','with','to','by','ml','pcs','pieces','l','liters','litre','litres']),
  lv: new Set(['plus','mīnus','minus','pievieno','pieliec','palielini','atņem','atnem','noņem','nonem','samazini','un','ar','uz','par','no','l','ml','gab','gabali']),
  zh: new Set(['加','添加','增加','加上','减','減','减去','減去','去掉','减少','減少','和','的','个','個','升','毫升'])
};

function normalizeText(s){
  return String(s || '')
    .toLowerCase()
    .replace(/ё/g, 'е')
    .replace(/[^\p{L}\p{N}]+/gu, ' ')
    .trim();
}

function parseZhNumber(s){
  const digits = { '零':0,'〇':0,'一':1,'二':2,'两':2,'兩':2,'三':3,'四':4,
                   '五':5,'六':6,'七':7,'八':8,'九':9 };
  const units = { '十':10,'百':100,'千':1000,'万':10000 };
  let total = 0, section = 0, number = 0;
  for (const ch of s){
    if (digits[ch] !== undefined){ number = digits[ch]; }
    else if (units[ch] !== undefined){
      const u = units[ch];
      if (u === 10000){ section = (section + number) * u; total += section; section = 0; number = 0; }
      else { if (number === 0) number = 1; section += number * u; number = 0; }
    }
  }
  return total + section + number;
}

function extractNumber(text){
  const digits = text.match(/\d+/);
  if (digits) return parseInt(digits[0], 10);

  if (curLang() === 'zh'){
    const zh = String(text).match(/[零〇一二两兩三四五六七八九十百千万萬]+/);
    return zh ? parseZhNumber(zh[0].replaceAll('萬','万')) : null;
  }

  const map = NUM_WORDS[curLang()] || RU_NUMS;
  const words = normalizeText(text).split(/\s+/);
  let sum = 0, section = 0, found = false;
  for (const w of words){
    const n = map[w];
    if (n !== undefined){
      if (n === 100) section = Math.max(1, section) * 100;
      else if (n >= 1000) { sum += Math.max(1, section) * n; section = 0; }
      else section += n;
      found = true;
    }
  }
  return found ? sum + section : null;
}

function interpretVoice(raw, lang, items) {
  currentLanguage = lang;
  const text = normalizeText(raw);
  const words = VOICE_WORDS[lang] || VOICE_WORDS.en;
  if (words.stop.test(text)) return { type: 'stop' };
  const plus = words.plus.test(text) || /\+/.test(raw);
  const minus = words.minus.test(text) || /-/.test(raw);
  if (plus && minus) return { type: 'unknown' };
  const names = items.filter(item => text.includes(normalizeText(item.name)));
  if (names.length > 1) return { type: 'ambiguous' };
  if (plus || minus) {
    const number = extractNumber(names.length ? text.replace(normalizeText(names[0].name), '') : raw);
    return { type: 'count', id: names[0]?.id, delta: (minus ? -1 : 1) * (number === null ? 1 : number) };
  }
  if (names.length === 1) return { type: 'select', id: names[0].id };
  return { type: 'unknown' };
}
let currentLanguage = 'en';
function curLang() { return currentLanguage; }

// ---- src/app.js ----
const $ = id => document.getElementById(id);
const escape = value => String(value ?? '').replace(/[&<>"']/g, char => ({ '&':'&amp;', '<':'&lt;', '>':'&gt;', '"':'&quot;', "'":'&#39;' }[char]));
let store, blocked = false, selected = null, reordering = false, filter = '', detailId = null, toastTimer, recognition = null, pendingImport = null, waitingWorker = null;
const lang = () => store?.state.preferences.lang || 'en';
const t = key => translate(lang(), key);
const number = value => Number(value).toLocaleString(LANGS[lang()]?.locale || 'en');
const session = () => activeSession(store.state);
const nameOf = item => itemName(item, lang());
const categoryOf = category => categoryName(category, lang());
const dialog = $('dialog');
let storage;
try { storage = window.localStorage; }
catch { storage = { getItem:()=>null, setItem:()=>{throw new Error('Device storage is unavailable. Enable site storage before counting.');} }; }
function notify(message) { $('toast').textContent = message; $('toast').hidden = false; clearTimeout(toastTimer); toastTimer = setTimeout(() => $('toast').hidden = true, 3600); }
function failure(error) { $('warning').textContent = error.message; $('warning').hidden = false; $('save-status').textContent = 'Change not saved'; $('status-dot').style.background = 'var(--danger)'; const field = $('dialog-error'); if (field) field.textContent = error.message; }
function status() { if (!$('warning').hidden) return; $('save-status').textContent = t(navigator.onLine ? 'saved' : 'offline'); $('status-dot').style.background = ''; }
function commit(action) {
  if (blocked) { notify(t('recovery')); return false; }
  try {
    store.commit(applyAction(store.state, action)); $('warning').hidden = true;
    if (action.type === 'count' || action.type === 'set') renderCounts(); else render();
    status(); return true;
  } catch (error) { failure(error); return false; }
}
function count(id, delta) {
  selected = id;
  if (commit({ type:'count', id, delta })) {
    const tile = [...document.querySelectorAll('.tile')].find(tile => tile.dataset.id === id);
    tile?.classList.remove('bump'); requestAnimationFrame(() => tile?.classList.add('bump'));
    navigator.vibrate?.(8);
  }
}
function applyPreferences() {
  const p = store.state.preferences, root = document.documentElement;
  root.lang = p.lang;
  root.dataset.theme = p.theme === 'auto' ? (matchMedia('(prefers-color-scheme: light)').matches ? 'light' : 'dark') : p.theme;
  root.dataset.accent = p.accent; root.dataset.contrast = p.contrast;
  $('grid').style.setProperty('--grid-direction', p.gridDir);
  if (p.gridCols === 'auto') $('grid').style.removeProperty('--columns'); else $('grid').style.setProperty('--columns', p.gridCols);
  $('grid').style.setProperty('--custom-tile-font', ({ sm:'10px', md:'12px', lg:'16px' })[p.textSize]);
  $('grid').style.setProperty('--custom-quantity-font', ({ sm:'16px', md:'20px', lg:'27px' })[p.qtySize]);
  if (p.rowFit === 'auto') $('grid').style.removeProperty('--custom-tile-picture-height');
  else $('grid').style.setProperty('--custom-tile-picture-height', `${Math.max(40, Math.floor((innerHeight - 290) / Number(p.rowFit)) - 65)}px`);
  document.querySelector('meta[name="theme-color"]').content = root.dataset.theme === 'light' ? '#f7f8f2' : root.dataset.theme === 'oled' ? '#000000' : '#101512';
}
function render() {
  applyPreferences();
  for (const [id,key] of Object.entries({ 'sessions-button':'sessions', 'new-button':'newCount', 'collection-title':'yourItems', 'gesture-hint':'hint', 'add-button':'add', 'total-label':'total', 'selected-label':'selected', empty:'empty' })) $(id).textContent = t(key);
  $('undo-button').innerHTML = `↶ <span>${escape(t('undo'))}</span>`;
  document.querySelector('[data-action="settings"]').setAttribute('aria-label',t('settings.title'));
  $('redo-button').setAttribute('aria-label',t('redo'));
  $('reorder-button').setAttribute('aria-label',t('move'));
  $('search').placeholder = t('search'); $('search').setAttribute('aria-label',t('search'));
  $('reorder-button').setAttribute('aria-pressed',String(reordering));
  $('session-name').textContent = session().name || t('untitled');
  $('date').textContent = new Date(session().date + 'T12:00:00').toLocaleDateString(LANGS[lang()]?.locale, { day:'numeric',month:'long',year:'numeric' });
  renderGrid(); renderCounts(); status();
}
function renderGrid() {
  const groups = [{ id:null, name:t('ungrouped') }, ...session().categories];
  let visible = 0;
  $('grid').innerHTML = groups.map(category => {
    const items = session().items.filter(item => item.categoryId === category.id && (!filter || (!item.spacer && nameOf(item).toLocaleLowerCase().includes(filter))));
    if (!items.some(item => !item.spacer)) return '';
    visible += items.filter(item => !item.spacer).length;
    return `<section class="category"><div class="category-header" style="text-align:${store.state.preferences.catAlign}"><h2>${escape(category.id ? categoryOf(category) : category.name)}</h2><span><strong data-category-total="${escape(category.id || '')}">0</strong></span></div><div class="tiles">${items.map(item => item.spacer ? `<div class="tile spacer" aria-hidden="true"></div>` : `<article class="tile" data-id="${escape(item.id)}"><button class="count-button" data-action="count" data-id="${escape(item.id)}" aria-label="${escape(nameOf(item))}: +1"><span class="tile-picture">${item.image ? `<img src="${escape(item.image)}" alt="" draggable="false">` : '<span class="tile-placeholder">▤</span>'}</span><span class="tile-bottom"><span class="tile-name">${escape(nameOf(item))}</span><span class="tile-quantity">${number(item.qty)}</span></span></button><button class="tile-menu" data-action="details" data-id="${escape(item.id)}" aria-label="${escape(t('details') + ': ' + nameOf(item))}">···</button>${reordering ? `<div class="tile-order"><button data-action="move-up" data-id="${escape(item.id)}" aria-label="Move earlier">←</button><button data-action="move-down" data-id="${escape(item.id)}" aria-label="Move later">→</button></div>` : ''}</article>`).join('')}</div></section>`;
  }).join('');
  $('empty').hidden = visible > 0;
}
function renderCounts() {
  const sums = totals(session()); $('total').textContent = number(sums.quantity);
  $('progress-label').textContent = `${sums.counted} / ${sums.items} ${t('checked')}`;
  $('progress-bar').style.width = `${sums.items ? sums.counted / sums.items * 100 : 0}%`;
  $('undo-button').disabled = !store.canUndo; $('redo-button').disabled = !store.canRedo;
  for (const tile of document.querySelectorAll('.tile[data-id]')) {
    const item = session().items.find(item => item.id === tile.dataset.id);
    tile.querySelector('.tile-quantity').textContent = number(item.qty);
    tile.classList.toggle('checked', item.counted); tile.classList.toggle('selected', item.id === selected);
  }
  for (const element of document.querySelectorAll('[data-category-total]')) element.textContent = number(session().items.filter(item => !item.spacer && (item.categoryId || '') === element.dataset.categoryTotal).reduce((sum,item) => sum + item.qty,0));
  const item = session().items.find(item => item.id === selected && !item.spacer);
  $('quick-bar').hidden = !item;
  if (item) { $('selected-name').textContent = nameOf(item); $('selected-qty').textContent = number(item.qty); }
  const detail = session().items.find(item => item.id === detailId);
  if (detail && $('detail-qty')) $('detail-qty').value = detail.qty;
}
function open(title, content) {
  $('dialog-title').textContent = title; $('dialog-content').innerHTML = content + '<p id="dialog-error" class="dialog-error" role="alert"></p>';
  if (!dialog.open) dialog.showModal();
}
function close() { dialog.close(); detailId = null; }
const button = (action,label,css='',id='') => `<button type="button" data-action="${action}" data-id="${escape(id)}" class="${css}">${escape(label)}</button>`;
function confirm(title, hint, action, id='') { open(title, `<p>${escape(hint)}</p><div class="actions">${button('close',t('cancel'))}${button(action,t('confirm'),'primary',id)}</div>`); }
function details(id) {
  const item = session().items.find(item => item.id === id); if (!item) return;
  selected = id; detailId = id; renderCounts();
  open(nameOf(item), `<form id="quantity-form" class="form-stack"><label>${escape(t('quantity'))}<input id="detail-qty" class="counter-input" type="number" inputmode="numeric" min="0" max="999999999" step="1" required value="${item.qty}"></label><div class="counter-controls">${button('detail-minus','−1')}${button('detail-zero','0')}${button('detail-plus','+1')}</div><button class="primary" type="submit">${escape(t('set'))}</button></form><form id="bulk-form" class="bulk-panel"><label>${escape(t('bulk'))}</label><div class="bulk-inputs"><label>${escape(t('perRow'))}<input id="per-row" type="number" inputmode="numeric" min="1" max="999" required value="8"></label><span>×</span><label>${escape(t('rows'))}<input id="rows" type="number" inputmode="numeric" min="1" max="999" required value="9"></label></div><div id="bulk-result" class="bulk-result">+72</div><div class="actions"><button type="submit" class="primary">${escape(t('calc.add'))}</button>${button('edit-item',t('edit'),'',id)}</div></form>`);
}
function itemEditor(id) {
  const item = session().items.find(item => item.id === id);
  detailId = null;
  open(t(item ? 'editor.editItem' : 'add'), `<form id="item-form" data-id="${escape(id || '')}" class="form-stack"><label>${escape(t('name'))}<input name="name" required maxlength="160" value="${escape(item ? nameOf(item) : '')}"></label><label>${escape(t('categories'))}<select name="categoryId"><option value="">${escape(t('ungrouped'))}</option>${session().categories.map(category => `<option value="${escape(category.id)}" ${item?.categoryId === category.id ? 'selected' : ''}>${escape(categoryOf(category))}</option>`).join('')}</select></label><div class="actions">${item ? button('delete-item',t('remove'),'danger',id) : ''}<button type="submit" class="primary">${escape(t('editor.save'))}</button></div></form>`);
}
function categories() {
  open(t('categories'), `<div>${session().categories.map(category => `<div class="category-row"><span>${escape(categoryOf(category))}</span>${button('edit-category',t('edit'),'',category.id)}${button('delete-category',t('remove'),'danger',category.id)}</div>`).join('')}</div><div class="actions">${button('add-category',t('cats.add'),'primary')}</div>`);
}
function categoryEditor(id) {
  const category = session().categories.find(category => category.id === id);
  open(t(category ? 'editor.editCategory' : 'editor.newCategory'), `<form id="category-form" data-id="${escape(id || '')}" class="form-stack"><label>${escape(t('name'))}<input name="name" required maxlength="160" value="${escape(category ? categoryOf(category) : '')}"></label><button type="submit" class="primary">${escape(t('editor.save'))}</button></form>`);
}
function settings() {
  const p = store.state.preferences;
  const select = (key,label,options) => `<label>${escape(t(label))}<select name="${key}">${options.map(([value,text]) => `<option value="${value}" ${p[key] === value ? 'selected' : ''}>${escape(text)}</option>`).join('')}</select></label>`;
  const option = values => values.map(value => [value,t('settings.' + value)]);
  open(t('settings.title'), `<form id="settings-form"><div class="settings-grid">${select('lang','settings.language',[['en','English'],['lv','Latviešu'],['ru','Русский'],['zh','中文']])}${select('theme','settings.theme',[['auto',t('settings.themeAuto')],['light',t('settings.themeLight')],['dark',t('settings.themeDark')],['oled','OLED']])}${select('accent','settings.accent',['green','blue','violet','pink','amber','cyan'].map(value => [value,t('accent.'+value)]))}${select('gridCols','settings.cols',['auto','3','4','5','6'].map(value=>[value,value === 'auto' ? t('settings.auto') : value]))}${select('rowFit','settings.rowsVisible',['auto','3','4','5','6'].map(value=>[value,value === 'auto' ? t('settings.auto') : value]))}${select('textSize','settings.textSize',[['sm',t('settings.small')],['md',t('settings.medium')],['lg',t('settings.large')]])}${select('qtySize','settings.qtySize',[['sm',t('settings.small')],['md',t('settings.medium')],['lg',t('settings.large')]])}${select('gridDir','settings.direction',[['ltr',t('settings.ltr')],['rtl',t('settings.rtl')]])}${select('contrast','settings.contrast',[['normal',t('settings.contrastNormal')],['high',t('settings.contrastHigh')]])}${select('catAlign','settings.catAlign',[['left',t('settings.alignLeft')],['center',t('settings.alignCenter')],['right',t('settings.alignRight')]])}</div><div class="actions"><button type="submit" class="primary">${escape(t('apply'))}</button></div></form>`);
}
function more() {
  const entries = [['csv','export'],['backup','backup','backupHint'],['restore','restore','restoreHint'],['categories','categories'],['voice',recognition ? 'stopVoice' : 'voice','voiceHint'],['reset','reset','resetHint']];
  open('Count', `<div class="menu-list">${entries.map(([action,label,hint])=>`<button data-action="${action}">${escape(t(label))}${hint?`<small>${escape(t(hint))}</small>`:''}</button>`).join('')}</div>`);
}
function sessionList() {
  open(t('sessions'), store.state.sessions.map(value=>`<div class="session-row"><button data-action="switch-session" data-id="${escape(value.id)}" ${value.id===store.state.activeId?'class="primary"':''}>${escape(value.name||t('untitled'))}<small>${escape(value.date)} · ${number(totals(value).quantity)}</small></button></div>`).join(''));
}
function nameForm(isNew) { open(t(isNew ? 'newCount' : 'name'), `<form id="${isNew ? 'new-session' : 'rename'}-form" class="form-stack">${isNew ? `<p>${escape(t('newHint'))}</p>`:''}<label>${escape(t('name'))}<input name="name" maxlength="160" value="${escape(isNew ? '' : session().name)}" autofocus></label><button type="submit" class="primary">${escape(t('confirm'))}</button></form>`); }
function download(data, filename, type) {
  const url = URL.createObjectURL(new Blob([data], {type})); const link = document.createElement('a'); link.href=url; link.download=filename; link.click(); setTimeout(()=>URL.revokeObjectURL(url),10000);
}
function history(type) { try { if (blocked) return; store[type](); selected=null; $('warning').hidden=true; render(); } catch(error) { failure(error); } }
function voice() {
  if (recognition) { recognition.stop(); recognition=null; notify(t('stopVoice')); return; }
  const Speech = window.SpeechRecognition || window.webkitSpeechRecognition;
  if (!Speech) { notify(t('voice.unsupported')); return; }
  const current = new Speech(); recognition=current; current.lang=LANGS[lang()]?.locale || 'en-US'; current.continuous=true; current.interimResults=false;
  current.onresult = event => {
    if (dialog.open || blocked) return;
    for (let i=event.resultIndex;i<event.results.length;i++) {
      if (!event.results[i].isFinal) continue;
      const command=interpretVoice(event.results[i][0].transcript,lang(),session().items.filter(item=>!item.spacer).map(item=>({...item,name:nameOf(item)})));
      if (command.type==='stop') { current.stop(); break; }
      if (command.type==='select') { selected=command.id; renderCounts(); notify(nameOf(session().items.find(item=>item.id===selected))); }
      else if (command.type==='count') { if (command.id || selected) count(command.id || selected,command.delta); else notify(t('voice.selectFirst')); }
      else notify(t('voiceHint'));
    }
  };
  current.onerror=()=>{ notify(t('voiceHint')); current.stop(); };
  current.onend=()=>{ if(recognition===current) recognition=null; $('more-button').classList.remove('voice-active'); };
  try { current.start(); $('more-button').classList.add('voice-active'); notify(t('voiceHint')); } catch(error) { recognition=null; failure(error); }
}
let suppressClick=false, press=null;
$('grid').addEventListener('pointerdown',event=>{
  const target=event.target.closest('.count-button'); if(!target || event.button!==0) return;
  suppressClick=false;
  press={ id:target.dataset.id, x:event.clientX,y:event.clientY,pointer:event.pointerId };
  press.timer=setTimeout(()=>{if(press){suppressClick=true;details(press.id);press=null;}},500);
});
document.addEventListener('pointermove',event=>{
  if(!press || press.pointer!==event.pointerId) return;
  const dx=event.clientX-press.x,dy=event.clientY-press.y;
  if(Math.abs(dx)>10 || Math.abs(dy)>10){clearTimeout(press.timer);press.moved=true;}
});
document.addEventListener('pointerup',event=>{
  if(!press || press.pointer!==event.pointerId) return;
  clearTimeout(press.timer);
  const dx=event.clientX-press.x,dy=event.clientY-press.y;
  if(press.moved){suppressClick=true;if(Math.abs(dx)>44 && Math.abs(dx)>Math.abs(dy)*1.5) count(press.id,-1);}
  press=null;
});
document.addEventListener('pointercancel',()=>{if(press)clearTimeout(press.timer);press=null;suppressClick=true;});
$('grid').addEventListener('contextmenu',event=>{if(event.target.closest('.count-button'))event.preventDefault();});
document.addEventListener('click',event=>{
  const target=event.target.closest('[data-action]'); if(!target) return;
  const {action,id}=target.dataset;
  if(action==='count' && suppressClick && event.detail!==0){suppressClick=false;return;}
  const selectedItem=()=>session().items.find(item=>item.id===selected);
  switch(action){
    case 'count': count(id,1); break;
    case 'details': details(id);break;
    case 'selected-minus': if(selectedItem())count(selected,-1);break;
    case 'selected-plus': if(selectedItem())count(selected,1);break;
    case 'selected-details': details(selected);break;
    case 'detail-minus': count(detailId,-1);break;
    case 'detail-plus': count(detailId,1);break;
    case 'detail-zero': commit({type:'set',id:detailId,value:0});break;
    case 'close': close();break;
    case 'settings':settings();break;
    case 'more':more();break;
    case 'undo':history('undo');break;
    case 'redo':history('redo');break;
    case 'rename':nameForm(false);break;
    case 'new-session':nameForm(true);break;
    case 'sessions':sessionList();break;
    case 'switch-session':if(commit({type:'switch-session',id})){selected=null;filter='';$('search').value='';render();close();}break;
    case 'add-item':itemEditor();break;
    case 'edit-item':itemEditor(id);break;
    case 'delete-item':confirm(t('remove'),t('deleteHint'),'confirm-delete-item',id);break;
    case 'confirm-delete-item':if(commit({type:'delete-item',id}))close();break;
    case 'categories':categories();break;
    case 'add-category':categoryEditor();break;
    case 'edit-category':categoryEditor(id);break;
    case 'delete-category':open(t('remove'),`<p>${escape(t('deleteCategoryHint'))}</p><div class="actions">${button('remove-category-keep',t('cats.orphan'),'',id)}${button('remove-category-all',t('cats.delAll'),'danger',id)}</div>`);break;
    case 'remove-category-keep':if(commit({type:'delete-category',id}))categories();break;
    case 'remove-category-all':if(commit({type:'delete-category',id,deleteItems:true}))categories();break;
    case 'reorder':reordering=!reordering;render();if(reordering)notify(t('moveHint'));break;
    case 'move-up':commit({type:'move',id,direction:-1});break;
    case 'move-down':commit({type:'move',id,direction:1});break;
    case 'reset':confirm(t('reset'),t('resetHint'),'confirm-reset');break;
    case 'confirm-reset':if(commit({type:'reset'}))close();break;
    case 'backup':download(JSON.stringify(store.state,null,2),`count-backup-${session().date}.json`,'application/json');close();break;
    case 'csv':download(csv(store.state),`count-${session().date}.csv`,'text/csv;charset=utf-8');close();break;
    case 'restore':$('import-file').click();break;
    case 'confirm-import':try{store.commit(pendingImport);pendingImport=null;blocked=false;selected=null;$('warning').hidden=true;render();close();}catch(error){failure(error);}break;
    case 'voice':close();voice();break;
    case 'reload':if(waitingWorker)waitingWorker.postMessage('ACTIVATE_UPDATE');else location.reload();break;
    case 'raw-backup':try{download(JSON.stringify({v4:localStorage.getItem(STORAGE_KEY),v3:localStorage.getItem('countlist.v3')},null,2),'count-recovery.json','application/json');}catch(error){failure(error);}break;
  }
});
document.addEventListener('submit',event=>{
  if(!event.target.closest('dialog'))return;event.preventDefault();const form=event.target,data=new FormData(form);
  try{
    switch(form.id){
      case 'quantity-form':if(commit({type:'set',id:detailId,value:quantity($('detail-qty').value)}))close();break;
      case 'bulk-form':{const amount=quantity($('per-row').value)*quantity($('rows').value);if(commit({type:'count',id:detailId,delta:amount}))notify(`+${number(amount)}`);break;}
      case 'item-form':if(commit({type:'save-item',id:form.dataset.id,name:data.get('name'),categoryId:data.get('categoryId')}))close();break;
      case 'category-form':if(commit({type:'save-category',id:form.dataset.id,name:data.get('name')}))categories();break;
      case 'settings-form':if(commit({type:'preferences',value:Object.fromEntries(data)})){recognition?.stop();close();}break;
      case 'rename-form':if(commit({type:'rename-session',name:data.get('name')}))close();break;
      case 'new-session-form':if(commit({type:'new-session',name:data.get('name')})){selected=null;filter='';$('search').value='';render();close();}break;
    }
  }catch(error){failure(error);}
});
document.addEventListener('input',event=>{if(['per-row','rows'].includes(event.target.id))$('bulk-result').textContent=`+${number(Number($('per-row').value)*Number($('rows').value))}`;});
$('search').addEventListener('input',event=>{filter=event.target.value.trim().toLocaleLowerCase();renderGrid();renderCounts();});
$('import-file').addEventListener('change',async event=>{
  const file=event.target.files[0];event.target.value='';if(!file)return;
  try{if(file.size>10000000)throw new Error('Backup exceeds 10 MB.');const raw=JSON.parse(await file.text());pendingImport=raw.version===4?validateState(raw):migrateLegacy(raw);confirm(t('restore'),`${t('restoreHint')} (${pendingImport.sessions.length} ${t('sessions')})`,'confirm-import');}catch(error){failure(error);}
});
document.addEventListener('keydown',event=>{
  if(dialog.open || /INPUT|TEXTAREA|SELECT/.test(event.target.tagName))return;
  if((event.ctrlKey || event.metaKey) && event.key.toLowerCase()==='z'){event.preventDefault();history(event.shiftKey?'redo':'undo');}
});
dialog.addEventListener('close',()=>{detailId=null;});
matchMedia('(prefers-color-scheme: light)').addEventListener('change',()=>applyPreferences());
window.addEventListener('resize',()=>applyPreferences());
window.addEventListener('online',status);window.addEventListener('offline',status);
window.addEventListener('storage',event=>{if(event.key===STORAGE_KEY || event.key===null){blocked=true;failure(new Error('Another tab changed these counts. Reload to continue.'));}});
try{
  store=createStore(storage,loadState(storage,navigator.language.slice(0,2)));store.initialize();render();
}catch(error){
  if(!store){store=createStore(storage,{state:createState(navigator.language.slice(0,2)),raw:storage.getItem(STORAGE_KEY)});blocked=true;}
  render();failure(error);
  if(blocked)open(t('restore'),`<p>${escape(t('recovery'))}</p><div class="menu-list">${button('raw-backup',t('rawBackup'))}${button('restore',t('restore'),'primary')}</div>`);
}
if('serviceWorker' in navigator && location.protocol !== 'file:' && (!['127.0.0.1','localhost'].includes(location.hostname) || new URLSearchParams(location.search).has('offline-test'))){
  let changingController=false;
  navigator.serviceWorker.addEventListener('controllerchange',()=>{if(waitingWorker && !changingController){changingController=true;location.reload();}});
  const offerUpdate=worker=>{waitingWorker=worker;$('offline-label').innerHTML=`<button data-action="reload">${escape(t('reload'))}</button>`;};
  navigator.serviceWorker.register('sw.js').then(registration=>{
    if(registration.waiting)offerUpdate(registration.waiting);
    registration.addEventListener('updatefound',()=>{const worker=registration.installing;worker?.addEventListener('statechange',()=>{if(worker.state==='installed'){if(navigator.serviceWorker.controller)offerUpdate(worker);else $('offline-label').textContent=t('ready');}});});
    navigator.serviceWorker.ready.then(()=>{if(!waitingWorker)$('offline-label').textContent=t('ready');});
  }).catch(()=>{$('offline-label').textContent='Offline installation unavailable';});
}
})();
