{
  "name": "carldaris-sorter",
  "private": true,
  "version": "4.0.0",
  "type": "module",
  "scripts": {
    "build": "node scripts/build-standalone.cjs",
    "start": "npm run build && node scripts/serve.cjs",
    "test": "node --test tests/*.test.js",
    "audit:legacy": "node scripts/audit-legacy.cjs"
  }
}
